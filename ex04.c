#include <stdio.h> 

int main() 
{
    
    int a;
    int b;
    int c;
    int d;


printf("insert the ages (4) of the units\n");
scanf(" %d %d %d %d", &a, &b, &c, &d);
printf(" the sum of the ages is %d\n", a+b+c+d);
}